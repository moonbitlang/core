fn lex_decimal_integer(ctx : ParseContext, sign : Int, d1 : Int) -> Result[Double, ParseError] {
  abort("todo")
}

fn lex_decimal_point_leading(ctx : ParseContext, sign : Int) -> Result[Double, ParseError] {
  abort("todo")
}

fn lex_zero(ctx : ParseContext, sign : Int) -> Result[Double, ParseError] {
  abort("todo")
}
