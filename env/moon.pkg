import {
  "moonbitlang/core/builtin",
}

import {
  "moonbitlang/core/test",
} for "test"

options(
  targets: {
    "env_js.mbt": [ "js" ],
    "env_native.mbt": [ "native", "llvm" ],
    "env_wasm.mbt": [ "wasm", "wasm-gc" ],
  },
)
